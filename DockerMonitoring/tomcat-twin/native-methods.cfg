# Native methods to wrap: ClassName.methodName (see ConfigLoader).
#
# java.* / javax.* cannot be wrapped: the agent skips bootstrap packages
# (NativeWrapperTransformer.isSkippedPackage).
#
# When Tomcat loads tcnative/APR, these org.apache.tomcat.jni.Library natives
# run during startup — good for the Grafana "native methods" dashboard.

org.apache.tomcat.jni.Library.versionString
org.apache.tomcat.jni.Library.aprVersionString
